Asar File
  PsiComment(COMMENT_TOKEN)('; Negative test: whitespace between `?` and the suffix MUST NOT lex as a')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; macro label.')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)(';')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; The lexer patterns for MACRO_LABEL / MACRO_IDENTIFIER / MACRO_LOCAL_LABEL /')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; MACRO_PLUS_LABEL / MACRO_MINUS_LABEL all require `?` to be immediately')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; adjacent to the following character (no whitespace allowed). When')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; whitespace intervenes, the `?` has no matching rule on its own — it')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; should be reported as BAD_CHARACTER (since `?` is not a standalone token')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; in Asar; it only appears as part of `?=` or the macro-label tokens).')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)(';')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; This file is expected to produce multiple BAD_CHARACTER markers, NOT')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; macro-label PSI elements. The parser tree should still recover and')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiComment(COMMENT_TOKEN)('; continue past each bad line.')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiElement(NEWLINE_TOKEN)('\n')
  AsarLineImpl(LINE)
    AsarItemImpl(ITEM)
      AsarDirectiveImpl(DIRECTIVE)
        AsarMacroDefinitionImpl(MACRO_DEFINITION)
          PsiElement(IDENTIFIER_TOKEN)('macro')
          PsiWhiteSpace(' ')
          PsiElement(IDENTIFIER_TOKEN)('broken_syntax')
          AsarMacroParamListImpl(MACRO_PARAM_LIST)
            PsiElement(LPAREN_TOKEN)('(')
            PsiElement(RPAREN_TOKEN)(')')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  PsiComment(COMMENT_TOKEN)('; `? Foo:` — NOT a macro label (space between `?` and `Foo:`)')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  AsarUnknownTokenImpl(UNKNOWN_TOKEN)
    PsiElement(BAD_CHARACTER)('?')
  PsiWhiteSpace(' ')
  AsarLineImpl(LINE)
    AsarItemImpl(ITEM)
      AsarLabelDefinitionImpl(LABEL_DEFINITION)
        PsiElement(LABEL_TOKEN)('Foo:')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  PsiComment(COMMENT_TOKEN)('; `? .Sub` — NOT a macro sub-label (space between `?` and `.Sub`)')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  AsarUnknownTokenImpl(UNKNOWN_TOKEN)
    PsiElement(BAD_CHARACTER)('?')
  PsiWhiteSpace(' ')
  AsarLineImpl(LINE)
    AsarItemImpl(ITEM)
      AsarLocalLabelDefinitionImpl(LOCAL_LABEL_DEFINITION)
        PsiElement(LOCAL_LABEL_TOKEN)('.Sub')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  PsiComment(COMMENT_TOKEN)('; `? +` and `? -` — NOT macro anonymous labels (space between `?` and `+`/`-`)')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  AsarUnknownTokenImpl(UNKNOWN_TOKEN)
    PsiElement(BAD_CHARACTER)('?')
  PsiWhiteSpace(' ')
  AsarLineImpl(LINE)
    AsarItemImpl(ITEM)
      AsarAnonymousLabelDefinitionImpl(ANONYMOUS_LABEL_DEFINITION)
        AsarPlusLabelImpl(PLUS_LABEL)
          PsiElement(PLUS_TOKEN)('+')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  AsarUnknownTokenImpl(UNKNOWN_TOKEN)
    PsiElement(BAD_CHARACTER)('?')
  PsiWhiteSpace(' ')
  AsarLineImpl(LINE)
    AsarItemImpl(ITEM)
      AsarAnonymousLabelDefinitionImpl(ANONYMOUS_LABEL_DEFINITION)
        AsarMinusLabelImpl(MINUS_LABEL)
          PsiElement(MINUS_TOKEN)('-')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  PsiComment(COMMENT_TOKEN)('; A well-formed macro label after the broken lines, to confirm that')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  PsiComment(COMMENT_TOKEN)('; the parser recovers and the rest of the macro still parses cleanly.')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('    ')
  AsarLineImpl(LINE)
    AsarItemImpl(ITEM)
      AsarMacroLabelDefinitionImpl(MACRO_LABEL_DEFINITION)
        PsiElement(MACRO_LABEL_TOKEN)('?GoodLabel:')
  PsiElement(NEWLINE_TOKEN)('\n')
  PsiWhiteSpace('        ')
  AsarLineImpl(LINE)
    AsarItemImpl(ITEM)
      AsarImpliedInstructionImpl(IMPLIED_INSTRUCTION)
        PsiElement(IMPLIED_OPCODE_TOKEN)('rts')
  PsiElement(NEWLINE_TOKEN)('\n')
  AsarLineImpl(LINE)
    AsarItemImpl(ITEM)
      AsarDirectiveImpl(DIRECTIVE)
        AsarEndmacroDirectiveImpl(ENDMACRO_DIRECTIVE)
          PsiElement(IDENTIFIER_TOKEN)('endmacro')